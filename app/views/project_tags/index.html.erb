<h1>Listing project_tags</h1>

<table>
  <tr>
  </tr>

<% @project_tags.each do |project_tag| %>
  <tr>
    <td><%= link_to 'Show', project_tag %></td>
    <td><%= link_to 'Edit', edit_project_tag_path(project_tag) %></td>
    <td><%= link_to 'Destroy', project_tag, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New project_tag', new_project_tag_path %>